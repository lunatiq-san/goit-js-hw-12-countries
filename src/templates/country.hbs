<div class="card">
  <h1>{{name}}</h1>
  <div class="card-body">
    <h2 class="card-title">Capital: {{capital}}</h2>
    <p class="card-text">Population: {{population}}</p>

    <p class="card-text"><b>Languages:</b></p>
    <ul class="list-group"></ul>
      {{#each languages}}
        <li class="list-group-item">{{name}}</li>
      {{/each}}
    </ul>
  </div>
  <div class="card-img">
    <img src="{{flag}}" alt="{{name}}">
  </div>
</div>